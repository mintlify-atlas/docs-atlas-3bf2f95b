---
title: "List Scans"
description: "GET /api/v1/scans — Retrieve all scans for the authenticated user."
---

## Endpoint

```
GET https://code-security-platform.onrender.com/api/v1/scans
```

Authentication required. Returns all scans belonging to the authenticated user, ordered by `created_at` descending (newest first).

## Response

### 200 — Success

Returns an array of scan objects. Each object includes the full `findings` array.

<ResponseField name="id" type="string" required>
  Unique scan identifier (UUID).
</ResponseField>

<ResponseField name="user_id" type="string" required>
  UUID of the user who created the scan.
</ResponseField>

<ResponseField name="status" type="string" required>
  Current scan status. One of `queued`, `running`, `completed`, or `failed`.
</ResponseField>

<ResponseField name="repo_url" type="string">
  Repository URL that was scanned. `null` for local scans.
</ResponseField>

<ResponseField name="is_local" type="boolean" required>
  `true` if this was a local file upload scan.
</ResponseField>

<ResponseField name="created_at" type="string" required>
  ISO 8601 timestamp of when the scan was created.
</ResponseField>

<ResponseField name="completed_at" type="string">
  ISO 8601 timestamp of when the scan finished. `null` until completed.
</ResponseField>

<ResponseField name="risk_score" type="number">
  Overall risk score on a 0–10 scale. `null` until the scan completes.
</ResponseField>

<ResponseField name="findings" type="array">
  List of security findings associated with the scan.
</ResponseField>

## Example

<RequestExample>

```bash cURL
curl --request GET \
  --url https://code-security-platform.onrender.com/api/v1/scans \
  --header 'Authorization: Bearer <JWT_TOKEN>'
```

</RequestExample>

<ResponseExample>

```json 200
[
  {
    "id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "user_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "repo_url": "https://github.com/owner/repo",
    "is_local": false,
    "zip_path": null,
    "status": "completed",
    "created_at": "2024-01-15T10:30:00Z",
    "completed_at": "2024-01-15T10:32:45Z",
    "risk_score": 7.4,
    "findings": [
      {
        "id": "f1a2b3c4-d5e6-7890-abcd-ef1234567890",
        "scan_id": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
        "title": "Hard-coded secret detected",
        "severity": "HIGH",
        "description": "A hard-coded API key was found in the source code.",
        "remediation": "Move the secret to an environment variable.",
        "scanner_name": "bandit",
        "file_path": "src/config.py",
        "line_number": 12,
        "cve_id": null,
        "confidence": "HIGH",
        "is_fixed": false,
        "pr_url": null
      }
    ]
  }
]
```

</ResponseExample>
